import * as THREE from "three";
import ScreenPoint from "./screenPoint";

function drawLineAlgorithm() {
  let startPosition = new THREE.Vector3(0, 0, 0);
  let endPosition = new THREE.Vector3(50, 50, 50);
}
